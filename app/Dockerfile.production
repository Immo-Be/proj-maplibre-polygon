# build stage
FROM --platform=linux/amd64 node:lts-alpine as build
WORKDIR /usr/src/app
COPY package*.json ./
RUN npm install
COPY . .
EXPOSE 4173
RUN npm run build
# assumes the default SvelteKit host and port
ENV ORIGIN=http://194.164.203.81


CMD ["node", "build"]
