// This is the name of the docker container running the db
// We use docker-compose to take advantage of the docker network automatically created be compose
export const PB_URL = 'http://pocketbase_db:8090';
